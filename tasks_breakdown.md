# План работ по улучшению бота

Этот документ описывает 9 задач по исправлению ошибок и добавлению нового функционала.

---

### Задача 1: Исправить кнопку "Редактировать"

**Проблема:** Кнопка "Редактировать" для созданного поста перестает функционировать.
**Причина:** Отсутствует `callback_query` обработчик для `cm_edit_post_{post_id}`.
**Файлы для модификации:**
-   [`bot/handlers/content_maker_handler.py`](bot/handlers/content_maker_handler.py): Добавить обработчики для начала и завершения редактирования.
-   [`bot/utils/states.py`](bot/utils/states.py): Добавить состояние `post_editing`.
-   [`bot/services/content_posts_service.py`](bot/services/content_posts_service.py): Добавить метод для обновления текста поста.

---

### Задача 2: Добавить кнопку "Назад" после создания поста

**Проблема:** Когда готовый пост показывается пользователю, нет кнопки "Назад" для возврата в главное меню "Контент мейкера".
**Причина:** В клавиатуре `get_post_actions_keyboard` уже есть кнопка "Назад" с `callback_data="cm_main"`. Проблема может быть в том, что она не отображается или не работает как ожидается.
**Файлы для модификации:**
-   [`bot/keyboards/keyboards.py`](bot/keyboards/keyboards.py:208): Проверить и убедиться, что кнопка "Назад" (`[InlineKeyboardButton(text="🔙 Назад", callback_data="cm_main")]`) корректно добавляется.
-   [`bot/handlers/content_maker_handler.py`](bot/handlers/content_maker_handler.py:124): Убедиться, что обработчик `@router.callback_query(F.data == "cm_main")` правильно отрабатывает и возвращает в главное меню.

---

### Задача 3: Реализовать постраничный просмотр идей

**Проблема:** Идеи выводятся списком, нет удобной навигации для их просмотра.
**Причина:** Текущая реализация выводит первые 6 идей одним сообщением и добавляет для них кнопки. Нет логики для пагинации.
**Файлы для модификации:**
-   [`bot/handlers/content_maker_handler.py`](bot/handlers/content_maker_handler.py:614): Переделать логику отображения идей. Вместо отправки нескольких сообщений, редактировать одно, показывая по одной идее с кнопками-стрелками.
-   [`bot/keyboards/keyboards.py`](bot/keyboards/keyboards.py): Создать новую клавиатуру `get_idea_navigation_keyboard(current_index, total_ideas)` с кнопками "⬅️", "➡️", "Сохранить", "Выбрать" и "Назад".

---

### Задача 4: Реализовать очистку чата

**Проблема:** В разделе "Контент мейкер" накапливаются старые сообщения, загромождая чат.
**Причина:** Отсутствует механизм отслеживания и удаления предыдущих сообщений при навигации.
**Файлы для модификации:**
-   [`bot/handlers/content_maker_handler.py`](bot/handlers/content_maker_handler.py): В ключевых обработчиках (меню, генерация идей, показ поста) сохранять ID важных сообщений в `state` и удалять их при переходе на новый шаг. Например, `await bot.delete_message(chat_id, message_id)`.

---

### Задача 5: Исправить кнопку "Напиши пост"

**Проблема:** В главном меню "Контент мейкера" не работает кнопка "Напиши пост".
**Причина:** Кнопка имеет `callback_data="cm_write_post"`, но в `content_maker_handler.py` отсутствует обработчик для этого `callback_data`.
**Файлы для модификации:**
-   [`bot/handlers/content_maker_handler.py`](bot/handlers/content_maker_handler.py): Добавить новый обработчик `@router.callback_query(F.data == "cm_write_post")`, который будет инициировать процесс создания поста (например, показывать выбор источника: из идей, из планера, своя идея).

---

### Задача 6: Удалить раздел "Кейсы"

**Проблема:** Необходимо убрать из интерфейса раздел "Кейсы".
**Причина:** Раздел присутствует в клавиатуре типов контента.
**Файлы для модификации:**
-   [`bot/keyboards/keyboards.py`](bot/keyboards/keyboards.py:169): Удалить `InlineKeyboardButton(text="📊 Кейсы", callback_data="cm_type_15")`. и логику связаную с кейсами если она есть. 

---

### Задача 7: Улучшить "Планер"

**Проблема:** В "Планере" нет сортировки по типам контента, счетчиков и удобной навигации.
**Причина:** Реализация очень базовая: просто выводится список последних 10 идей.
**Файлы для модификации:**
-   [`bot/services/content_ideas_service.py`](bot/services/content_ideas_service.py): Создать новый метод для группировки идей по `content_type_id` и подсчета их количества.
-   [`bot/handlers/content_maker_handler.py`](bot/handlers/content_maker_handler.py:865): Полностью переработать логику `show_planner`. Сначала показывать меню с типами контента и количеством идей в каждом, а затем, по выбору, отображать список идей конкретного типа с пагинацией.
-   [`bot/keyboards/keyboards.py`](bot/keyboards/keyboards.py): Создать новые клавиатуры для отображения категорий в планере и для навигации по идеям внутри категории.

---

### Задача 8: Исправить ошибку `MultipleResultsFound`

**Проблема:** При добавлении третьего голосового фрагмента возникает ошибка `sqlalchemy.exc.MultipleResultsFound`.
**Причина:** Запрос в `ContentProfileService.add_voice_chunk` для получения последнего фрагмента (`result.scalar_one_or_none()`) возвращает несколько строк. Вероятно, не хватает `ORDER BY` и `LIMIT`.
**Файлы для модификации:**
-   [`bot/services/content_profile_service.py`](bot/services/content_profile_service.py:250): Изменить запрос к БД, который ищет `last_chunk`. Он должен быть отсортирован по полю `chunk_order` (или аналогичному) в порядке убывания и ограничен одной записью (`.order_by(VoiceChunk.chunk_order.desc()).limit(1)`).

---

### Задача 9: Исправить ошибку `TypeError: 'type' object is not subscriptable`

**Проблема:** При завершении голосовой сессии возникает `TypeError` в `whisper_service.py`.
**Причина:** Используется синтаксис `list[str]`, который доступен только в Python 3.9+, а проект, вероятно, работает на более старой версии (например, 3.8).
**Файлы для модификации:**
-   [`bot/services/whisper_service.py`](bot/services/whisper_service.py:90): Заменить аннотацию типа `list[str]` на `typing.List[str]`. В начале файла необходимо добавить `from typing import List`.